from django.conf.urls import url

from nad_app import views

urlpatterns = [

    # url(r'^reserve', views.LanguageViewSet.as_view(), name='reserve'),
    # url(r'^in', views.LanguageViewSet.as_view(), name='in'),
    # url(r'^out', views.LanguageViewSet.as_view(), name='out'),

]
